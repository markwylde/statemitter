{
  "name": "statemitter",
  "version": "1.0.1",
  "description": "A library for observing when state changes",
  "main": "test.js",
  "scripts": {
    "start": "esbuild --bundle example/index.js --outfile=example/index.min.js --minify --sourcemap & servatron -p 8080 -d example",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "state",
    "state management"
  ],
  "author": {
    "name": "Mark Wylde",
    "email": "me@markwylde.com",
    "url": "https://github.com/markwylde"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.12.9",
    "servatron": "^1.1.3"
  },
  "dependencies": {
    "events": "^3.3.0"
  }
}
